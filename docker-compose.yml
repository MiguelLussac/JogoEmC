services:
  minddrop:
    build:
      context: .
      dockerfile: Dockerfile
    image: minddrop:latest
    container_name: minddrop_game
    environment:
      DISPLAY: ${DISPLAY:-host.docker.internal:0.0}
    volumes:
      - ./resources:/app/resources
    stdin_open: true
    tty: true
